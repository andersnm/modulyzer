{
  "name": "waveweb",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "vite": "vite --force",
    "build": "tsc && vite build",
    "build-pages": "tsc && vite build --mode pages",
    "preview": "vite preview"
  },
  "dependencies": {
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "@types/pako": "^2.0.3",
    "typescript": "^5.5.2",
    "vite": "^5.0.8"
  }
}
